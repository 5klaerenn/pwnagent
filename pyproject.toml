[project]
name = "pwnagent"
version = "0.1.0"
description = "Agent IA pour triage des challenges de pwn"
requires-python = ">=3.12"
dependencies = [
    "pwntools",
    "httpx",
]

[project.scripts]
pwnagent = "agent.core:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
